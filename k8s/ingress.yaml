apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: urlshortner-ingress
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: python-service
                port:
                  number: 5000
